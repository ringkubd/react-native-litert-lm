<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.reactnativelitertlm">

    <!--
      LiteRT-LM requires at least Android 7.0 (API 24).
      AI Edge LiteRT delegates may require additional capabilities,
      but we detect this at runtime — no hard requirement here.
    -->
    <uses-sdk android:minSdkVersion="26" />

</manifest>
